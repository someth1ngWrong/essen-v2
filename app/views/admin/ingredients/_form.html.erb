<%= form_with(model: [:admin, ingredient]) do |form| %>
  <% if ingredient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ingredient.errors.count, "error") %> prohibited this ingredient from being saved:</h2>

      <ul>
        <% ingredient.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ingredient %>
    <%= form.text_field :ingredient %>
  </div>

  <div class="field">
    <%= form.label 'Замена ингредиента' %>
    <%= collection_check_boxes(:ingredient, :replacement_ids, Ingredient.all, :id, :ingredient, include_hidden: false) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
